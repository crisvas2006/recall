# Pin exact versions during first setup (uv pip compile / pip freeze).
# Left unpinned here because versions drift; 

# Web
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv

# LLM + embeddings
google-genai

# Reranker (CPU)
sentence-transformers

# Postgres / Supabase (pgvector via SQL)
psycopg[binary,pool]
pgvector

# Observability (OpenTelemetry -> Phoenix)
opentelemetry-sdk
opentelemetry-exporter-otlp
arize-phoenix-otel

# Evaluation
ragas
